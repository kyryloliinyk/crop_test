<h1><%= @field.name %></h1>
<p>Area: <%= @field.area %> square meters</p>
<p>Shape (GeoJSON):</p>
<pre><%= JSON.pretty_generate(@field_geojson) %></pre>

<%= link_to('<– all fields', fields_path)%>
